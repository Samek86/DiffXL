# 07 内容同期スクロール・画像完全対応

本ドキュメントは **ポインタ** です。実装タスクの本体は次を参照してください。

**実装計画書（本体）:**  
[`docs/superpowers/plans/2026-08-11-perfect-content-scroll-image-alignment.md`](../../docs/superpowers/plans/2026-08-11-perfect-content-scroll-image-alignment.md)

**状態:** **実装完了**（2026-08-11 Task 1–7）。回帰ゲートは下表 DoD を参照。

## 何を完成させるか（要約）

1. **描画前**にシート内の全画像を類似度行列＋最適 1:1 割当で対応付ける  
2. 各画像の **セル占有範囲（from〜to）** を drawing から取得する  
3. 縦スクロールは内容マップ、横はセル位置 1:1  
4. 片側のみ画像はギャップ（相手ホールド）、再一致で同期  
5. 専用テストデータ `content_scroll_*.xlsx` + 期待 JSON + 三段自動テスト

## 関連要件

- C-07 片側のみイメージ  
- C-08 テキストによる連結  
- C-09 内容理解に基づく比較  
- N-01 同期スクロール  

## 実装後の状態（ギャップ解消）

| 項目 | 完成形 |
|------|--------|
| 画像マッチ | 類似度行列 + Hungarian 最適 1:1（`ImageCorrespondenceService`） |
| 占有セル | `AnchorRect` from〜to（twoCell / oneCell） |
| スクロール | `SheetAlignment` / `ContentScrollMap` 単一路線 |
| テスト | content_scroll + PerfectSmoke + auto-live 三段 |

## 完了の定義（DoD）— 回帰結果 2026-08-11

| # | 項目 | 結果 |
|---|------|------|
| 1 | 事前マッチ・ログ | 実装済（Alignments / 対応表） |
| 2 | full_feature IMG-C/D 誤ペア 0 | **PASS** Image=1 OnlyL=1 OnlyR=1 |
| 3 | twoCell `RowEnd>=RowStart` | 実装済（AnchorRect / PerfectSmoke span） |
| 4 | SC_画像ギャップ hold | **PASS** auto-live + PerfectSmoke |
| 5 | same_B 再同期 | **PASS** |
| 6 | 横同期 列一致 | **PASS** HSCROLL_OK |
| 7 | T1/T2/T3 | **PERFECT_SCROLL_PASS** / **AUTO_LIVE_PASS** |
| 8 | full_feature auto-live | **AUTO_LIVE_PASS** FAILURES=0 |

テストケース: `10_管理資料/テスト/テストケース一覧.md` の **TC-CS-01〜12**。  
ハーネス: `10_管理資料/テスト/run-content-scroll-test.ps1`。