<UserControl
    x:Class="DiffXL.VIEW.Controls.WorkbookPane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:DiffXL.VIEW.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="400"
    d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Padding="8,6"
            Background="{StaticResource Brush.BgMuted}"
            BorderBrush="{StaticResource Brush.Border}"
            BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,8,0" VerticalAlignment="Center">
                    <iconPacks:PackIconPhosphorIcons
                        Kind="FileXls"
                        Width="14"
                        Height="14"
                        Margin="0,0,6,0"
                        VerticalAlignment="Center"
                        Foreground="{StaticResource Brush.Text}" />
                    <TextBlock
                        VerticalAlignment="Center"
                        FontWeight="SemiBold"
                        Foreground="{StaticResource Brush.Text}"
                        Text="{Binding PaneTitle, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                </StackPanel>

                <TextBlock
                    x:Name="PathText"
                    Grid.Column="1"
                    Margin="0,0,8,0"
                    VerticalAlignment="Center"
                    TextTrimming="CharacterEllipsis"
                    ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                    Text="（未選択）"
                    Foreground="{StaticResource Brush.TextMuted}" />

                <ComboBox
                    x:Name="SheetCombo"
                    Grid.Column="2"
                    Width="148"
                    Margin="0,0,8,0"
                    VerticalAlignment="Center"
                    IsEnabled="False"
                    ToolTip="ツールバーのシート対応に合わせて左右が切り替わります"
                    SelectionChanged="SheetCombo_SelectionChanged" />

                <Button
                    x:Name="OpenButton"
                    Grid.Column="3"
                    MinWidth="72"
                    Click="OpenButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconPhosphorIcons Kind="ArrowSquareOut" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center" />
                        <TextBlock Text="開く" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- 内容ビュー（Excel 埋め込みは廃止。ContentPane が主表示） -->
        <Border
            Grid.Row="1"
            Background="#FFF9FAFB"
            ClipToBounds="True"
            SnapsToDevicePixels="True">
            <Grid
                x:Name="HostContainer"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                ClipToBounds="True">
                <local:ContentPane
                    x:Name="ContentHost"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
